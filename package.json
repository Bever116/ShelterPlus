{
  "name": "shelterplus",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.4",
  "devDependencies": {
    "prisma": "5.9.1"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "echo 'Use pnpm --filter apps/web dev or apps/api dev'",
    "lint": "pnpm -r lint"
  }
}
